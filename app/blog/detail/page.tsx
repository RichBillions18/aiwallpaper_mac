/**
 * 【第2阶段 · 路由 /blog/detail】app/blog/detail/page.tsx
 *
 * 没有 "use client" → 服务端组件，只展示静态内容，不需要 hooks。
 * 和 /blog 的区别：多一层 detail 文件夹，URL 多一段路径。
 */
export default function () {
  return <div>blog detail</div>;
}
